$(document).ready(() => {
  // initialize paroller.js and set attributes
  $("#entete").paroller({
    factor: "0.5",
    type: "background"
    // coverRatio: 1
    // direction: "vertical"
  });
});
